#include "main.h"

/**
 * jack_bauer - Print the minutes of a day
 *
 * Return : no return
 */
void jack_bauer(void)
{
	int i = hours;
	int j = minutes
		for (i = 0; i < 24; i++)
		{
			for (j = 0; j < 60; j++)
			{
				_putchar(j);
			}
			_putchar(i);
			_putchar('\n');
		}
}		
